services:
  - type: web
    name: ai-symptom-intake
    env: python
    rootDir: Task1/generated_project
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
